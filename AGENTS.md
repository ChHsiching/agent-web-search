## Agent skills

### Issue tracker

Issues live as GitHub issues in this repo (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles map 1:1 to same-named GitHub labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
